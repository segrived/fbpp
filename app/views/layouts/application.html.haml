!!! 5

%html
  %head
    %title Feedback++
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    :coffeescript
      I18n.defaultLocale = "#{I18n.default_locale}"
      I18n.locale = "#{I18n.locale}"
    = csrf_meta_tags
  %body
    / Шапка сайта
    #header
      #headline
        %p= link_to "Feedback", :root
      #menu
        %ul
          - unless exists_admin_account?
            = nav_link t('layout.menu.create_admin'), :create_admin, :id => 'create_admin_account_link'
          = nav_link t('layout.menu.home'), :root
          = nav_link t('layout.menu.about'), :about
          = nav_link t('layout.menu.faq'), :faq
          - if logged?
            - if can_admin?
              = nav_link t('layout.menu.admin'), :admin_root
              = nav_link t('layout.menu.messages'), :inbox
              -if (messages = unread_messages_count) > 0
                %span#unread-messages-count= "(#{unread_messages_count})"
            = nav_link t('layout.menu.departaments'), :departaments
            = nav_link t('layout.menu.profile'), :profile
            = nav_link t('layout.menu.logout'), :logout
          -else
            = nav_link t('layout.menu.register'), :register
            = nav_link t('layout.menu.login'), :login
    / Контент страницы
    #content-box
      #content
        = yield
    / Подвал сайта
    #footer
      %p#left-text
        Feedback++, 2013
      %p#right-text
        Команда разработчиков
        %b «Quart»