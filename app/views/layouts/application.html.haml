!!! 5

%html
  %head
    %title Feedback++
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    :coffeescript
      I18n.defaultLocale = "#{I18n.default_locale}"
      I18n.locale = "#{I18n.locale}"
    = csrf_meta_tags
  %body
    / Шапка сайта
    #header
      #headline
        %p= link_to "Feedback", :root
      #menu
        %ul
          - unless exists_admin_account?
            %li= nav_link t('layout.menu.create_admin'), :create_admin, 'create_admin_account_link'
          %li= nav_link t('layout.menu.home'), :root
          %li= nav_link t('layout.menu.about'), :about
          %li= nav_link t('layout.menu.faq'), :faq
          - if logged?
            %li= nav_link t('layout.menu.messages'), :inbox
            -if (messages = unread_messages_count) > 0
              %span#unread-messages-count= "(#{unread_messages_count})"
            - if can_admin?
              %li= nav_link t('layout.menu.admin'), :admin_root
            %li= nav_link t('layout.menu.departaments'), :departaments
            %li= nav_link t('layout.menu.profile'), :profile
            %li= nav_link t('layout.menu.logout'), :logout
          -else
            %li= nav_link t('layout.menu.register'), :register
            %li= nav_link t('layout.menu.login'), :login
    / Контент страницы
    #content-box
      #content
        = yield
    / Подвал сайта
    #footer
      %p#left-text
        Feedback++, 2013
      %p#right-text
        Команда разработчиков
        %b «Quart»